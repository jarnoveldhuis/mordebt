specVersion: "v1beta"
serviceId: "mordebt"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mordebt-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
